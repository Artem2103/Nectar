# Nectar MVP

## Foundation

- [x] Create Flutter project
- [x] Create folder structure (feature-first: app/, core/, features/)
- [x] Create theme (Material 3 customised, Inter bundled)
- [x] Create design system (colors, spacing, radii, shadows, typography tokens)

## Navigation

- [x] Bottom navigation (custom floating bar, GoRouter StatefulShellRoute)
- [x] Floating action button (add-meal entry point)

## Screens

- [x] Home (header, weekly calendar, nutrition/activity stat carousel, recently-uploaded empty state)
- [x] Progress (streak + badges, current-weight journey, weight-trend chart)
- [x] Groups (maintenance placeholder)
- [x] Profile (maintenance placeholder)

## Components

- [x] App card (reusable rounded surface + muted variant)
- [x] Section / screen titles
- [x] Pill button
- [x] Maintenance view
- [x] Nutrition card (macro stat card + calories card)
- [x] Calendar strip
- [x] Health score card
- [x] Progress bar (reusable linear fill)
- [x] Streak card / Badges card
- [x] Current weight card
- [x] Weight chart card (line chart + range selector)
- [x] Meal card (name, image thumbnail, macros, time)

---

## Phase 1 — Data Layer  (foundation; everything else depends on this)

- [x] Add packages: supabase_flutter, image_picker, http, uuid
- [x] MealEntry model  (id, timestamp, name, imagePath, kcal, protein, carbs, fat, fiber, sugar, sodium)
- [x] WeightEntry model  (id, timestamp, valueKg)
- [x] UserGoals model  (dailyKcal, proteinG, carbsG, fatG, fiberG, sugarG, sodiumMg, startWeightKg, goalWeightKg)
- [x] MealRepository  — CRUD, persisted to Supabase `meal_entries` (RLS-scoped per user)
- [x] WeightRepository — CRUD, persisted to Supabase `weight_entries` (RLS-scoped per user)
- [x] GoalsRepository  — read/upsert UserGoals to Supabase `user_goals` (RLS-scoped per user)
- [x] Riverpod providers for each repository (AsyncNotifierProvider)
- [x] dailySummaryProvider: read today's meals from MealRepository, compute remaining macros vs UserGoals
- [x] progressSummaryProvider: read WeightRepository + MealRepository (streak logic)

## Phase 2 — Add Meal Flow

- [x] Wire FAB → /add-meal route (GoRouter push, not shell branch)
- [x] AddMealScreen: manual form (meal name, kcal, protein, carbs, fat, + optional fiber, sugar, sodium)
- [x] "Analyse photo" button: image_picker → send image to Claude API → auto-fill form fields
- [x] AddMealScreen shows AI-parsed result, allows edits, confirm button saves to MealRepository
- [x] On save: pop back to Home; provider invalidation triggers reactive rebuild
- [x] Meal card widget: show in "Recently uploaded" list on Home

## Phase 3 — Progress & Weight Logging

- [x] "Log weight" sheet on Progress screen (tap button → bottom sheet with number input)
- [x] Save WeightEntry to WeightRepository
- [x] Streak computed from MealRepository (days with ≥ 1 meal logged, consecutive from today)
- [x] WeightChartCard uses real WeightRepository data
- [x] Badge awards: 1-day streak, 7-day streak, first meal logged

## Phase 4 — User Goals / Profile

- [x] Profile screen: form to set daily kcal goal + macro targets
- [x] Start weight + goal weight entry (feeds Progress screen journey bar)
- [x] Persist via GoalsRepository; providers invalidate on change

---

## AI

- [x] image_picker + Claude API vision call in Add Meal flow (Phase 2)
- [x] Camera capture (alternative to gallery pick — bottom sheet: camera or gallery)
- [x] Result / confirmation with editable fields (in AddMealScreen)

## Backend

- [x] Authentication (Supabase email/password: login, register, sign-out)
- [x] Email-confirmation handling (verify-email screen + resend; onboarding waits for a session)
- [x] Cloud database (Supabase Postgres + RLS — replaces SharedPreferences repositories)
- [x] Image storage (meal photos: upload to Supabase Storage `meal_images` bucket; public URL stored in image_path)

## Polish

- [ ] Animations
- [x] Dark mode (bespoke widgets repaint via NectarColors ThemeExtension + context.colors)
- [ ] Different Languages
- [ ] Accessibility

---

## Bugs / Known issues (reported 2026-06-23)

- [x] Home: second carousel (NutritionCarousel) has a RenderFlex "overflow by 15 pixels" yellow/black stripe — fix the card/row height or content sizing
- [x] Home: carousel cards on adjacent pages touch when scrolling — need symmetric horizontal spacing/gutter between pages (viewportFraction + padding/peek)
- [ ] AI: Claude vision API not actually connected for "Analyse photo" — wire up the real API key/request (deferred)

---

## Phase 5 — Profile expansion (currently just goals form + sign out)

Restructure Profile into a hub: header → grouped settings list (tap row → detail screen / sheet).
- [x] Hub layout shipped: header + grouped SettingsSection rows + sign out
      (ProfileScreen rebuilt on the settings-list widgets).

### Account & profile
- [ ] Editable profile: display name, avatar photo (upload to Supabase Storage), email
- [ ] Personal details: age, sex, height, activity level (feeds smarter goal suggestions)
- [ ] Change password / change email
- [ ] Delete account (with confirmation)

### Goals & nutrition
- [x] Move goals form into a "Goals & targets" detail screen (GoalsScreen,
      pushed via /goals; macro targets already captured here)
- [x] Capture macro targets here (closes onboarding gap — protein/carbs/fat)
- [x] Capture micronutrient targets here (fiber / sugar / sodium) — feed the
      carousel's 2nd page (was hardcoded constants in dailySummaryProvider).
      REQUIRES adding fiber_g, sugar_g, sodium_mg columns to Supabase `user_goals`
      (updateGoals upserts the full row, so writes fail until the columns exist;
      reads are null-tolerant).
- [~] Units preference (metric / imperial — kg/lb, ml/oz)
      Preference is stored + selectable in Profile, but weight/volume displays
      don't convert yet — wire UnitSystem into Progress/weight UI next.
- [ ] Goal type (lose / maintain / gain) + weekly rate

### Preferences
- [x] Theme: light / dark / system (picker wired to themeController +
      MaterialApp.themeMode; full dark repaint shipped — see Dark mode)
- [ ] Language picker (ties into i18n polish item)
- [~] Notifications: meal reminders, weigh-in reminders, streak nudges (toggles)
      Toggles persist via settings; actual local-notification scheduling not
      wired yet.
- [ ] Start-of-week, date/time format

### Subscription & billing
- [ ] Subscription tier (free / premium) — paywall + manage subscription screen
- [ ] Restore purchases / billing portal link
- [ ] Premium gating for AI photo analysis (usage limits on free tier)

### About & support
- [ ] Help / FAQ, contact support
- [ ] Privacy policy + terms links
- [ ] App version / build info
- [ ] Rate the app / share

### Foundation needed
- [x] Settings model + repository (persist preferences to Supabase + local cache)
      AppSettings (theme/units/notification toggles); SettingsRepository =
      Supabase `user_settings` row + shared_preferences cache (remote wins,
      cache fallback offline). NOTE: requires a `user_settings` table in
      Supabase (RLS per user) with columns: user_id, theme_mode, units,
      meal_reminders, weigh_in_reminders, streak_nudges.
- [x] Reusable settings-list widgets (section header, nav row, toggle row, value row)
      lib/features/profile/presentation/widgets/settings_widgets.dart:
      SettingsSection / SettingsNavRow / SettingsToggleRow / SettingsValueRow.

---

## Session 2026-06-24 — micronutrient goals + dark-mode repaint

Done this session:

- [x] UserGoals: added fiberG / sugarG / sodiumMg (defaults 30 g / 50 g / 2300 mg);
      copyWith + toJson/fromJson + toSupabaseJson/fromSupabaseJson all updated and
      null-tolerant on read.
- [x] GoalsScreen form: new second macro row — Fiber g / Sugar g / Sodium mg —
      under the protein/carbs/fat row; wired to controllers, dispose, and save.
- [x] dailySummaryProvider: carousel's 2nd page (fiber/sugar/sodium "left") now
      reads UserGoals instead of the deleted hardcoded _fiberGoalG/_sugarGoalG/
      _sodiumGoalMg constants; guards divide-by-zero on zero goals.
- [x] AddMealScreen: optional Fiber / Sugar / Sodium fields (blank → 0 via new
      _Field.optional flag so they don't block save); saved onto MealEntry.
- [x] meal_analyser: MealAnalysis gained fiberG/sugarG/sodiumMg; vision prompt
      asks for them (grams; sodium in mg) and they auto-fill on "Analyse photo".
- [x] Dark mode now repaints bespoke widgets:
      * New NectarColors ThemeExtension (lib/core/theme/nectar_colors.dart) holds
        the flipping surface/text/line tokens; registered in AppTheme.light/dark
        via extensions:. Access through context.colors.<token> (NectarColorsX).
      * Migrated ~25 widgets from AppColors.<flippingToken> → context.colors.*,
        de-consting decorations as needed; two CustomPainters (weight chart, day
        ring) now take their colours as constructor params.
      * AppTypography._base no longer bakes a colour — primary text follows the
        themed default text style; AppTheme.light applies bodyColor/displayColor.
      * Brand colours (accent/streak/water) + mid-grey textSecondary/textTertiary
        stay AppColors constants. White icons on always-orange backgrounds (home
        delete swipe, streak check) intentionally kept AppColors.onInverse.
- [x] flutter analyze clean (only the pre-existing anonKey deprecation in main.dart).

ACTION REQUIRED (Supabase, manual): add columns to `user_goals` before saving
goals will work —
    alter table user_goals
      add column fiber_g   double precision default 30,
      add column sugar_g   double precision default 50,
      add column sodium_mg double precision default 2300;
- [x] Theme controller (Riverpod) wired to MaterialApp themeMode
      themeControllerProvider drives MaterialApp.themeMode; AppTheme.dark added.
      Bespoke token-driven widgets now migrated to NectarColors/context.colors,
      so dark mode fully repaints (see Dark mode polish item).
